package org.workshop.cart;

import org.workshop.cart.dao.ProductDao;
import org.workshop.cart.model.Product;

public class MyCartTest {
	public static void main(String arg[]) {
		/*ProductDao dao = new ProductDao();
		Product product = new Product("C5", "ProductUpdated", "Test Desciption", 200, null);
		try {
			dao.create(product);
			//dao.delete(product.getCode());
		} catch (Exception e) {
			e.printStackTrace();
		}*/
		System.out.println(Double.parseDouble("350.0"));
	}
}
